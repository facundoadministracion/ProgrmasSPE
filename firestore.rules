rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Esta regla permite leer y escribir a CUALQUIER usuario (incluso anónimo)
    // Ideal para desarrollo. En producción las ajustaremos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
